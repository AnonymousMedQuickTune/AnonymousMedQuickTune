training:
  voxel_calculation: "median"
  learning_rate: 1e-4
  gradient_accumulation_steps: 1
  optimizer_type: "adam"
  weight_decay: 1e-4  
  warmup_epochs: 0
  label_smoothing: 0.0
  mixup_alpha: 0.0
